using UnityEngine;

[RequireComponent(typeof(PlayerMovement))]
[RequireComponent(typeof(PlayerAttack))]
public class Player : Unit
{
    private void Start()
    {
        // FindTarget<Enemy>();
    }
}